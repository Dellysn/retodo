import { DashboardLayout } from '@/components/layout/dashboard-layout';

const CreateNotePage = () => (
    <div>CreateNotePage</div>
  );

export default CreateNotePage;
CreateNotePage.PageLayout = DashboardLayout;
